# Dice Rolling Game 🎲

A simple Python project built for learning basic programming and problem-solving concepts.

## Features
- User inputs choice to roll the dice
- Random dice roll using Python `random` module
- Displays results of two dice
- Handles invalid input

## How to Run
1. Open `main.py` in Python
2. Run the program
3. Follow the on-screen instructions
